@model Asr.Models.Slot

@using Microsoft.AspNetCore.Identity
@inject UserManager<User> UserManager
@{
    ViewData["Title"] = "Edit";
}

<h2>Booking / Unbooking</h2>

<h3>Are you sure you want to book/unbook this?</h3>
<div>
    <h4>Slot</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RoomID)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.RoomID)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.StartTime)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.StartTime)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.StaffID)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.StaffID)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.StudentID)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.StudentID)
        </dd>
    </dl>

    <form asp-action="Edit">
        <input type="hidden" asp-for="RoomID" />
        <input type="hidden" asp-for="StartTime" value="@Model.StartTime.ToString("yyyy/MM/dd HH:mm")" />
        @*<input type="hidden" asp-for="@UserManager.GetUserName(User)" />*@
        <input type="submit" value="Booking/Unbooking" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>

